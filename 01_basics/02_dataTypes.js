"use strict"; //treat all JS code as newer version
// alert(3+2) // we are using node js not browser

let userName = "Bilal"
let age = 22
let isLoggedIn = false
let state = null

        // Primitive DataTypes
//number
//bigint    => for large nubers
//string => perfer "" doublequote
//boolean => true/false
// null => standalone value
// undefined => not defined any value
// symbol => for unique

    // Non-Primitive DataTypes
// objects

console.table([typeof userName,typeof age,typeof isLoggedIn,typeof state])

// typeod undefined //is undefined
// typeof null //is Object